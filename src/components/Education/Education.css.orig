.education-section {
  width: 100vw;
  background-color: #0e0e11;
  color: #ffffff;
  padding: 8rem 2rem;
  display: flex;
  justify-content: center;
  position: relative;
  z-index: 10;
}

.education-container {
  max-width: 1100px;
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 6rem;
}

.section-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: 1.5rem;
}

.education-title {
  font-size: clamp(2.5rem, 4vw, 3.5rem);
  font-weight: 600;
  margin: 0;
}

.education-subtitle {
  font-size: 1.1rem;
  color: #a0a0a0;
  margin: 0;
  max-width: 500px;
}

/* Timeline Layout */
.timeline {
  position: relative;
  max-width: 1000px;
  margin: 0 auto;
  padding: 2rem 0;
  width: 100%;
}

/* Center Line */
.timeline::after {
  content: '';
  position: absolute;
  width: 2px;
  background: linear-gradient(to bottom, transparent, #0885FE, #0885FE, transparent);
  top: 0;
  bottom: 0;
  left: 50%;
  margin-left: -1px;
}

.timeline-item {
  padding: 10px 40px;
  position: relative;
  width: 50%;
  box-sizing: border-box;
  opacity: 0;
  transform: translateY(40px);
  transition: all 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

.timeline-item.animate-in {
  opacity: 1;
  transform: translateY(0);
}

.timeline-item.left {
  left: 0;
}

.timeline-item.right {
  left: 50%;
}

/* Icon Node */
.timeline-dot {
  position: absolute;
  width: 50px;
  height: 50px;
  right: -25px;
  background: #0e0e11;
  border: 2px solid #0885FE;
  top: calc(50% - 25px); /* Center vertically relative to the item */
  border-radius: 50%;
  z-index: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #0885FE;
  box-shadow: 0 0 20px rgba(8, 133, 254, 0.4);
  transition: all 0.4s ease;
}

.timeline-item.right .timeline-dot {
  left: -25px;
  right: auto;
}

.timeline-item:hover .timeline-dot {
  background: #0885FE;
  color: #fff;
  transform: scale(1.15);
  box-shadow: 0 0 30px rgba(8, 133, 254, 0.7);
}

/* Modern Card Design */
.timeline-card {
  padding: 2.5rem;
  background: rgba(255, 255, 255, 0.02);
  border: 1px solid rgba(255, 255, 255, 0.05);
  position: relative;
  border-radius: 20px;
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  overflow: hidden;
  box-shadow: 0 10px 30px rgba(0,0,0,0.2);
}

.timeline-card:hover {
  background: rgba(255, 255, 255, 0.04);
  border-color: rgba(8, 133, 254, 0.4);
  transform: translateY(-5px);
  box-shadow: 0 15px 40px rgba(0,0,0,0.5), 0 0 20px rgba(8, 133, 254, 0.15);
}

/* Card Content Typography */
.timeline-date {
  display: inline-block;
  font-size: 0.9rem;
  font-weight: 600;
  color: #0885FE;
  letter-spacing: 1px;
  background: rgba(8, 133, 254, 0.1);
  padding: 0.4rem 1.2rem;
  border-radius: 20px;
  margin-bottom: 1.5rem;
  transition: all 0.3s ease;
}

.timeline-card:hover .timeline-date {
  background: #0885FE;
  color: #fff;
  box-shadow: 0 0 15px rgba(8, 133, 254, 0.5);
}

.timeline-title {
  font-size: 1.6rem;
  font-weight: 600;
  color: #ffffff;
  margin: 0 0 0.5rem 0;
  line-height: 1.3;
  transition: color 0.3s ease;
}

.timeline-card:hover .timeline-title {
  color: #0885FE;
}

.timeline-company {
  font-size: 1.1rem;
  font-weight: 400;
  color: #a0a0a0;
  margin: 0 0 1rem 0;
}

.timeline-desc {
  font-size: 1rem;
  color: #888;
  line-height: 1.6;
  margin: 0;
}

/* Hover Overlay for Certificates */
.timeline-overlay {
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(8, 133, 254, 0.9);
  display: flex;
  justify-content: center;
  align-items: center;
  opacity: 0;
  backdrop-filter: blur(4px);
  -webkit-backdrop-filter: blur(4px);
  transition: all 0.4s ease;
  z-index: 10;
  border-radius: inherit;
}

.timeline-card:hover .timeline-overlay {
  opacity: 1;
}

.view-cert-btn {
  display: inline-flex;
  align-items: center;
  background: #fff;
  color: #121212;
  padding: 0.8rem 1.8rem;
  border-radius: 40px;
  font-size: 1rem;
  font-weight: 600;
  text-decoration: none;
  transform: translateY(20px);
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  box-shadow: 0 10px 25px rgba(0,0,0,0.2);
}

.timeline-card:hover .view-cert-btn {
  transform: translateY(0);
}

.view-cert-btn:hover {
  background: var(--accent-color, #FFC107);
  transform: translateY(-3px) scale(1.05) !important;
}

/* Responsive Timeline */
@media (max-width: 900px) {
  .timeline::after {
    left: 40px;
  }
  
  .timeline-item {
    width: 100%;
    padding-left: 90px;
    padding-right: 0;
    margin-bottom: 2rem;
  }
  
  .timeline-item.left, .timeline-item.right {
    left: 0;
  }
  
  .timeline-dot {
    left: 15px; /* 40px - 25px */
    top: 40px; /* Align to top of card instead of center */
  }
  
  .timeline-item.left .timeline-dot, .timeline-item.right .timeline-dot {
    left: 15px;
    right: auto;
  }
}

@media (max-width: 768px) {
  .education-section {
    padding: 4rem 1.5rem;
  }
  
  .timeline::after {
    left: 20px;
  }
  
  .timeline-item {
    padding-left: 60px;
  }
  
  .timeline-dot {
    width: 40px;
    height: 40px;
    left: 0; /* 20px - 20px */
  }
  
  .timeline-item.left .timeline-dot, .timeline-item.right .timeline-dot {
    left: 0;
  }
  
  .timeline-card {
    padding: 1.5rem;
  }
  
  .timeline-title {
    font-size: 1.3rem;
  }
}
